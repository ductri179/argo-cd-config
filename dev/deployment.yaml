apiVersion: apps/v1
kind: Deployment
metadata:
  name: geo-deployment
spec:
  selector:
    matchLabels:
      app: geo
  replicas: 2
  template:
    metadata: 
      labels:
        app: geo
    spec:
      containers:
        - name: geo
          image: nanajanashia/argocd-app:1.0
          ports:
          - containerPort: 8080