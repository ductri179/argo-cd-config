apiVersion: apps/v1
kind: Deployment
metadata:
  name: geo-deployment
spec:
  selector:
    matchLabels:
      app: geo
  replicas: 2
  template:
    metadata: 
      labels:
        app: geo
    spec:
      volumes: 
        - name: demo-pv-storage
          persistentVolumeClaim:
            claimName: demo-pv-claim
      containers:
        - name: geo
          image: busybox
          ports:
          - containerPort: 8080
          volumeMounts:
            - mountPath: "/data"
              name: demo-pv-storage